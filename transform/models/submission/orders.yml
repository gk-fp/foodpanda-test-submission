models:
  - name: orders
    description: >
      Orders Schema
    meta:
      owner: candidate@foodpanda.com
    columns:
      - name: rdbms_id
        description: ID of relational database management system to get this data.
      - name: country_name
        description: Name of country.
      - name: date_local
        description: Local date information.
      - name: vendor_id
        description: ID of vendor.
      - name: customer_id
        description: ID of customer.
      - name: total_gmv
        description: GMV calculated for each country.
      - name: is_voucher_used
        description: Whether voucher is used or not.
      - name: is_successful_order
        description: Whether order is successful or not.
